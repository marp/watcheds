{% extends "base.html.twig" %}
{% block body %}
    <h1>Password reset - new</h1>
    {{ form_start(form) }}
    {{ form_widget(form)}}
    <button class="my-3 btn btn-primary" type="submit"><i class="fas fa-key"></i> Submit</button>
    {{ form_end(form) }}
{% endblock %}